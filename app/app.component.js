'use strict';

angular.module('help').
    component('help', {       
        controller: function($scope){
            // $scope.open = !open;
            // $scope.feedbacks = true;
            // $scope.ard = 'ltemvetrv';
        }
    });